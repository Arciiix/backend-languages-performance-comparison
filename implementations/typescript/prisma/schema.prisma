generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:example@localhost:3306/Test"
}

model Example {
  id     Int  @id @default(autoincrement())
  number Int?
}
